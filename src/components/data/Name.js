import React from 'react';
import classes from './Name.module.css';

const Name = (name) => {
    return(
            <div className={classes.name}>
                
            </div>
    );

};

export default Name;